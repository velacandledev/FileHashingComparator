# FileHashingComparator
This project allows to get the hashing of two different files, and compare their hashing.

While it can detect file updates, one said updates are reversed, the hashing will not reverse.
